//program to find the length of the string
#include <stdio.h>

int main ()
{
    char name [] = "my name is sambeet !";
    printf("the length of the string %s is %d\n", name, strlen(name));
    return 0;
}

